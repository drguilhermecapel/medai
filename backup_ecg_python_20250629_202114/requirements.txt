# Core dependencies
# numpy==1.24.3  # Comentado - revisar se usado apenas para ECG
# tensorflow==2.13.0  # Comentado - revisar se usado apenas para ECG
pydicom==2.4.3
opencv-python==4.8.1.78
Pillow>=10.0.0
# matplotlib>=3.7.0  # Comentado - revisar se usado apenas para ECG
# scikit-learn>=1.3.0  # Comentado - revisar se usado apenas para ECG
# torch>=2.0.0  # Comentado - revisar se usado apenas para ECG
# torchvision>=0.15.0  # Comentado - revisar se usado apenas para ECG

# GUI
PyQt5==5.15.9
pyqtgraph>=0.13.0

# Web server
flask>=3.0.0
flask-cors>=4.0.0
werkzeug>=3.0.0

# Medical imaging
SimpleITK>=2.3.0
nibabel>=5.1.0
scikit-image>=0.21.0

# Utilities
pandas>=2.0.0
h5py>=3.9.0
reportlab>=4.0.0
cryptography>=41.0.0
pyyaml>=6.0
psutil>=5.9.0

# Development
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0
